import numpy as np

# Settings
mu = 2  # Mean
sigma = np.sqrt(3)  # Standard deviation
num_samples = 1000  # Number of samples

# Generate Gaussian noise
noise = np.random.normal(mu, sigma, num_samples)

# Display noise samples
print(noise)
